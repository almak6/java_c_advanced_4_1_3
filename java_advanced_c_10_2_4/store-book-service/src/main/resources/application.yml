spring:
  cloud:
    stream:
      kafka:
        binder:
          auto-add-partitions: true
      bindings:
        bookSubscribing:
          destination: check-book
          group: group
          binder: kafka